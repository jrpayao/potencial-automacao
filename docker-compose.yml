services:
  api:
    build: { context: ., target: api }
    ports: ["3000:3000"]
    volumes: ["./data:/app/data"]
  frontend:
    build: { context: ., target: frontend }
    ports: ["80:80"]
    depends_on: [api]
